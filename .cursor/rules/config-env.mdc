---
description: 环境变量与 configs 目录维护约定（.env.example 与 .env 同步）
globs:
  - configs/**
  - internal/platform/config/**
alwaysApply: false
---

# 配置与环境变量

## 必读

- 模板：[`configs/.env.example`](../../configs/.env.example)
- 本地副本：[`configs/.env`](../../configs/.env)
- JSON 字段说明：[`configs/README.md`](../../configs/README.md)
- 架构文档：[`docs/architecture/config.md`](../../docs/architecture/config.md)

## 修改 .env.example 时必须

1. **同步** [`configs/.env`](../../configs/.env)：新增键、删键、改默认值或注释分组须与 example 一致（`.env` 可保留机器相关取值，但键集合不得落后）。
2. 若变量被 `internal/platform/config/config.go` 读取，同步更新：
   - `AppConfig` 字段与 `defaultConfig` / `merge` / `overrideWithEnv`
   - `configs/app.dev.json`、`configs/app.prod.json`
   - [`configs/README.md`](../../configs/README.md) 字段表
   - [`docs/architecture/config.md`](../../docs/architecture/config.md)
3. 在 [`.env.example`](../../configs/.env.example) 为新变量添加 **`#` 行内或分组注释**（用途、可选值、何时必填）。
4. 更新 [`CHANGELOG.md`](../../CHANGELOG.md) `[Unreleased]`。

## JSON 配置

- `app.*.json` **不支持** `#` 注释；说明写在 [`configs/README.md`](../../configs/README.md)，勿在 JSON 内写伪注释字段。
- dev 使用 Docker 服务名（`mysql`、`redis`、`rabbitmq`）；本机二进制用 `.env` 中 `127.0.0.1`。

## 禁止

- 只改 `.env.example` 不同步 `.env`
- 在 `.env` / example 中提交生产真实密钥
- 新增 config 字段却不更新 README 与 config.md
