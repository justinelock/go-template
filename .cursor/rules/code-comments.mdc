---
description: 新增或修改代码时必须写步骤级注释（越详细越好）
alwaysApply: true
---

# 步骤级注释

凡**新增或修改**业务/平台代码（含 Go、脚本、SQL），须补充与现有风格一致的**步骤级注释**。细则见 `docs/conventions/code-comments.md`。

## 必须注释的位置

| 位置 | 要求 |
|------|------|
| 导出类型、导出函数 | 块注释说明职责；复杂逻辑用 `1)` `2)` 或 `步骤 1：` 分步 |
| 结构体字段 | 每个字段一行：`// 步骤 N：` 或 `// 字段用途` |
| 函数体内 | **每个逻辑步骤**一行 `// 步骤 N：…`（校验、调用下游、错误映射、写响应等均单独成步） |
| 分支/循环 | 在分支入口注明步骤编号或「步骤 N.M」 |
| 非平凡脚本 | 每个阶段 `echo` 或 `# 步骤 N：` |

## 写法要求

- 用**中文**写注释（标识符、路径、错误码保持英文）。
- 步骤编号从 **1** 连续递增，避免跳号；嵌套逻辑可用 `步骤 3.1`。
- 说明**做什么 + 为什么**（例如「避免用户名探测」「gRPC 失败回退 HTTP」）。
- **禁止**用注释重复代码字面意思（坏：`// 调用 JSON`）；好：`// 步骤 4：按领域错误映射 HTTP 业务码，避免泄露用户是否存在`。
- 参考范例：`internal/gateway/transport/http/handler.go`、`internal/member/transport/http/handler.go`。

## 豁免（仍建议简要说明）

- `api/gen/**` 生成代码勿改注释。
- 单行 getter/纯委托且无任何分支时，可只保留函数块注释，不强制体内分步。

## 自检

提交前确认：新增/改动的函数体内是否逐步注释？导出符号是否有块注释？
