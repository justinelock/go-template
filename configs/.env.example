# =============================================================================
# go-template 本地环境变量模板（复制为 configs/.env 后按本机修改）
# 加载顺序见 docs/architecture/config.md；改键时须同步 configs/.env（含注释）
# =============================================================================

# --- 服务端口与运行环境 ---
GATEWAY_SERVICE_PORT=8180
MEMBER_SERVICE_PORT=8181
MEMBER_SERVICE_GRPC_PORT=9181
APP_ENV=dev
# CONFIG_FILE=configs/app.dev.json

# --- 网关下游地址（Consul 关闭时的静态回退）---
MEMBER_SERVICE_URL=http://127.0.0.1:8181
MEMBER_SERVICE_GRPC_ADDR=127.0.0.1:9181
GATEWAY_USE_MEMBER_GRPC=false

# --- order-service ---
ORDER_SERVICE_PORT=8182
ORDER_SERVICE_URL=http://127.0.0.1:8182

# --- payment-service ---
PAYMENT_SERVICE_PORT=8183
PAYMENT_SERVICE_URL=http://127.0.0.1:8183
# dev 允许 mock-pay；生产务必 false
PAYMENT_MOCK_PAY_ENABLED=true

# --- 可观测（日志 / 指标 / 链路）---
LOG_LEVEL=info
LOG_FORMAT=json
OTEL_ENABLED=false
# 启用 OTel 时取消注释，Compose obs profile 下示例：jaeger:4318
# OTEL_EXPORTER_OTLP_ENDPOINT=jaeger:4318

# --- 网关治理（限流 / 代理超时）---
RATE_LIMIT_ENABLED=false
RATE_LIMIT_REDIS_PREFIX=ratelimit
GATEWAY_PROXY_TIMEOUT_SEC=5

# --- MySQL ---
MYSQL_DSN=root:Root123456@tcp(127.0.0.1:3306)/go_template_db?charset=utf8mb4&parseTime=true&loc=Local

# --- Redis（token / 幂等 / 分布式锁 / 网关限流）---
REDIS_ADDR=127.0.0.1:6379
REDIS_PASSWORD=redis1234
REDIS_DB=0

# --- 消息队列（默认 rabbitmq；见 MQ_PROVIDER）---
MQ_PROVIDER=rabbitmq
MQ_AUTO_DECLARE=true
RABBITMQ_URL=amqp://guest:guest@127.0.0.1:5672/
ROCKETMQ_NAMESRV=127.0.0.1:9876

# --- Consul 服务发现（本地通常关闭）---
CONSUL_ENABLED=false
CONSUL_ADDRESS=127.0.0.1:8500
CONSUL_DATACENTER=dc1
SERVICE_HOST=127.0.0.1

# --- 跨域 ---
CORS_ALLOW_ORIGIN=http://localhost:5173
